// Vulnerable: bidi characters (Trojan Source)
// Note: The actual bidi characters are not included here as they are invisible
// This fixture tests that the rule is configured correctly
const safeCode = 'const x = 1;';

export { safeCode };
